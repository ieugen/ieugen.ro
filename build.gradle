plugins {
  id "com.github.ben-manes.versions" version "0.36.0"
}

repositories {
  maven {
    name = 'Clojars'
    url = 'https://repo.clojars.org/'
  }
}

description = 'Proiect ce construiește situl ieugen.ro'

subprojects {
    repositories {
        jcenter()
        mavenCentral()
        maven {
            name = 'Clojars'
            url = 'https://repo.clojars.org/'
        }
    }
}
